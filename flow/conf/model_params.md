# **Model Parameter Adjustments**
- `TEMP_{value}`
  - Sets **temperature** to `{value}`.
- `TOPP_{value}`
  - Sets **top-p** to `{value}`.
- **Default behavior**
  - Uses model defaults if `TEMP` or `TOPP` is not set.